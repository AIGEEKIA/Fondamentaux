# 🎨 DESIGN SYSTEM - STANDARDS UI/UX

## 🎯 **OBJECTIF**

Standards de design pour maintenir la cohérence visuelle et l'expérience utilisateur.

## 🎨 **PALETTE DE COULEURS OBLIGATOIRE**

### **COULEURS PRINCIPALES**

- **Bleu** : `blue-500/10` → `blue-600/10` (Introduction)
- **Vert** : `green-500/10` → `emerald-500/10` (Définition Simple)
- **Orange** : `orange-500/10` → `red-500/10` (Exemples Code)
- **Violet** : `purple-500/10` → `indigo-500/10` (Quiz)
- **Gris** : `gray-500/10` → `gray-600/10` (Navigation)

### **GRADIENTS STANDARD**

```css
/* Introduction */
bg-gradient-to-br from-blue-500/10 to-blue-600/10

/* Définition Simple */
bg-gradient-to-br from-green-500/10 to-emerald-500/10

/* Exemples Code */
bg-gradient-to-br from-orange-500/10 to-red-500/10

/* Mini-Application */
bg-gradient-to-br from-green-500/20 to-blue-600/20

/* Quiz */
bg-gradient-to-br from-purple-500/10 to-indigo-500/10
```

## 🧩 **COMPOSANTS UI OBLIGATOIRES**

### **CARD COMPONENT**

```tsx
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

// Structure standard
<Card className="bg-gradient-to-br from-[color]-500/10 to-[color]-600/10 border-2 border-[color]-300/50 shadow-xl">
  <CardHeader>
    <CardTitle className="text-2xl font-bold text-gray-800">
      🎯 Titre de Section
    </CardTitle>
    <CardDescription className="text-lg text-gray-600">
      Description de la section
    </CardDescription>
  </CardHeader>
  <CardContent>{/* Contenu de la section */}</CardContent>
</Card>;
```

### **ICÔNES LUCIDE REACT**

```tsx
import {
  Brain,
  BookOpen,
  Target,
  Play,
  CheckCircle,
  Copy,
  ChevronRight,
  ArrowRight,
} from "lucide-react";

// Utilisation standard
<div className="w-12 h-12 bg-gradient-to-br from-[color]-500 to-[color]-600 rounded-xl flex items-center justify-center shadow-lg">
  <IconName className="h-6 w-6 text-white" />
</div>;
```

### **BOUTONS STANDARD**

```tsx
// Bouton principal
<button className="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg transition-colors">
  Texte du bouton
</button>

// Bouton quiz
<button className={`w-full text-left p-4 rounded-lg border-2 transition-all duration-200 ${
  isAnswered
    ? index === quiz.correctAnswer
      ? "border-green-500 bg-green-50 text-green-800"
      : index === selectedAnswer
      ? "border-red-500 bg-red-50 text-red-800"
      : "border-gray-200 bg-gray-50 text-gray-600"
    : "border-gray-200 hover:border-purple-300 hover:bg-purple-50"
}`}>
```

### **HOVER EFFECTS**

```css
/* Boutons */
hover:bg-blue-700 transition-colors

/* Cards */
hover:scale-105 transition-transform duration-300

/* Liens */
hover:text-blue-600 transition-colors
```

### **TRANSITIONS STANDARD**

```css
/* Couleurs */
transition-colors

/* Transform */
transition-transform duration-300

/* Toutes les propriétés */
transition-all duration-200
```

## 🎯 **VALIDATION DESIGN**

1. **Palette respectée** : Couleurs conformes
2. **Composants cohérents** : shadcn/ui utilisés
3. **Responsive** : Mobile-first design
4. **Accessibilité** : Contrastes et focus
5. **Animations** : Transitions fluides

---

**Note** : Ces standards DOIVENT être respectés pour maintenir la cohérence visuelle du projet.
