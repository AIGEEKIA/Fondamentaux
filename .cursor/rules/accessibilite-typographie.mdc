# ♿ ACCESSIBILITÉ & TYPOGRAPHIE - STANDARDS UX

## 🎯 **OBJECTIF**

Standards d'accessibilité et de typographie pour une expérience utilisateur optimale.

## 📱 **RESPONSIVE DESIGN OBLIGATOIRE**

### **GRID SYSTEM**

```tsx
// Desktop : 3 colonnes, Mobile : 1 colonne
<div className="grid grid-cols-1 md:grid-cols-3 gap-6">

// Desktop : 2 colonnes, Mobile : 1 colonne
<div className="grid grid-cols-1 md:grid-cols-2 gap-4">
```

### **ESPACEMENT STANDARD**

```tsx
// Sections principales
<section className="mb-12">

// Contenu de section
<div className="space-y-6">

// Quiz
<div className="space-y-8">
```

## 🎭 **ANIMATIONS ET TRANSITIONS**

### **HOVER EFFECTS**

```css
/* Boutons */
hover:bg-blue-700 transition-colors

/* Cards */
hover:scale-105 transition-transform duration-300

/* Liens */
hover:text-blue-600 transition-colors
```

### **TRANSITIONS STANDARD**

```css
/* Couleurs */
transition-colors

/* Transform */
transition-transform duration-300

/* Toutes les propriétés */
transition-all duration-200
```

## ♿ **ACCESSIBILITÉ OBLIGATOIRE**

### **CONTRASTES**

- **Texte principal** : `text-gray-800`
- **Texte secondaire** : `text-gray-600`
- **Texte tertiaire** : `text-gray-400`
- **Liens** : `text-blue-600` avec `hover:text-blue-700`

### **FOCUS STATES**

```css
/* Focus visible pour tous les éléments interactifs */
focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2
```

### **ARIA LABELS**

```tsx
// Boutons avec aria-label
<button aria-label="Copier le code" onClick={copyToClipboard}>
  <Copy className="h-4 w-4" />
</button>
```

## 📐 **TYPOGRAPHIE STANDARD**

### **HIÉRARCHIE DES TITRES**

```tsx
// Titre principal
<h1 className="text-5xl font-bold text-white mb-2">

// Titre de section
<h3 className="text-3xl font-bold text-gray-800 mb-4">

// Titre de sous-section
<h4 className="text-xl font-semibold text-gray-800 mb-2">

// Titre de quiz
<h3 className="text-xl font-bold text-gray-800">
```

### **TEXTES STANDARD**

```tsx
// Description
<p className="text-lg text-gray-600">

// Contenu principal
<p className="text-gray-700 mb-4">

// Code
<code className="text-sm text-blue-600">
```

## 🎯 **VALIDATION ACCESSIBILITÉ**

1. **Contrastes** : WCAG 2.1 AA respecté
2. **Focus visible** : Tous les éléments interactifs
3. **Aria labels** : Boutons et liens accessibles
4. **Navigation clavier** : Tab order logique
5. **Responsive** : Mobile-first design

## 📊 **MÉTRIQUES ACCESSIBILITÉ**

- **Contraste** : Ratio minimum 4.5:1
- **Focus** : Visible sur tous les éléments
- **Navigation** : Clavier et souris
- **Screen readers** : Compatible
- **Mobile** : Touch-friendly

---

**Note** : Ces standards d'accessibilité et de typographie complètent le design system.
description:
globs:
alwaysApply: false

---
